# Emblem color-set stills

Capture the in-game Effect Description dialog for each color as `<color>.jpeg` — `gray.jpeg`, `pink.jpeg`, `navy.jpeg`. PNG and HEIC also work.

Thresholds live in the bundle. The wording after the number belongs in `src/ui/emblemSets.ts` `META`. Images stay gitignored.
